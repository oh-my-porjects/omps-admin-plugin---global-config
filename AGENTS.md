# 全局配置公共模块

- V4 后台页面由 `admin-intents.yaml`、`api-docs/` 和已发布项目意图共同决定；runtime 不读取仓库页面文件。
- 禁止使用 `admin-web.yaml`、`AdminWebHint` 或旧 spec 生成/回写流程。
- 配置值的值类型和 JSON 语义必须由真实接口验证；不得由页面猜测或将对象降级为字符串。
- 配置修改、重置属于受确认的真实写操作，接口、意图、审计和测试必须同步维护。
